{
    "version":2,
    "outputDirectory": "build",
    "builds": [
        {
            "src":"./server/app.js",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/server/app.js"
        }
    ]
}