{
    "version":2,
    "outputDirectory": "build",
    "builds": [
        {
            "src":"./server/app.js",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src":"/server/(.*)",
            "dest":"/server/app.js"
        }
    ]
}